# 馬太鞍 WebGIS 專案工作紀錄
**日期：** 2026 年 04 月 29 日
**負責開發與整合：** 昀慶 (政大國土服務團)
**核心目標：** 提升使用者互動體驗、深化地理圖資精準度、強化在地社群連結、並優化手機端瀏覽體驗。

---

## 🚀 重點更新項目摘要

### 1. 互動體驗升級 (UX/UI & Gamification)
*   **視覺回饋特效**：成功送出災情表單後，畫面會觸發全螢幕的彩帶慶祝特效 (`canvas-confetti`)，並彈出「感謝回報」的溫暖對話框，提升使用者的貢獻成就感。
*   **動態貢獻計數器**：在網頁標題區新增了具備動畫效果 (`popIn`) 的「社群共創貢獻進度」計數器，即時顯示目前累積的災情紀錄總數。
*   **地圖微互動**：
    *   **掉落與彈跳 (Drop & Bounce)**：新增的地圖標記點會有從天而降的彈跳特效。
    *   **脈衝發光 (Pulse Effect)**：標記點底座會散發脈衝發光特效，更容易在地圖上被辨識。
    *   **平滑聚焦 (FlyTo)**：送出表單後，地圖視角會自動且平滑地飛移至該次填報的座標位置。

### 2. 核心圖資與定位精準度提升 (Geospatial Upgrades)
*   **導入官方底圖**：實作了地圖右上角的「圖層切換器 (Layers Control)」，除了原本的 OpenStreetMap 外，正式介接了**花蓮縣政府通用電子地圖**與**高解析度正射影像 (衛星空照)** 作為底圖選項。
*   **串接進階疊加圖層 (Overlays)**：
    *   `[新增]` 都市計畫分區圖 (都市土地)
    *   `[新增]` 光復鄉地籍圖 (非都市土地 / 地政局資料)
    *   `[新增]` 全國段籍圖 (內政部國土測繪中心 NLSC 開放資料)
*   **Google Maps 門牌定位**：將原本的開源地址搜尋引擎 (Nominatim) 全面升級為精準度最高的 **Google Maps Geocoding API**，解決了台灣偏鄉門牌號碼無法精確定位的痛點，並結合環境變數 (`.env`) 保護金鑰安全。

### 3. 社群支持系統 (Community Engagement)
*   **正能量留言板 (`MessageBoard`)**：在地圖下方新增了專屬的社群留言板，提供災區居民情感交流的空間。
*   **匿名與隨機大頭貼**：支援使用者輸入自訂暱稱，系統會為每則留言隨機配對一個可愛的在地動物大頭貼（例如：🐸、🐻、🦌、🐗 等）。
*   **預設暖心罐頭訊息**：提供 6 款預設的正能量按鈕（如「馬太鞍辛苦了！」、「我們一起加油！」），降低居民留言門檻。
*   **資料庫雙軌運行**：支援正式連線模式 (Supabase) 與無資料庫的本地暫存模式，方便與負責後端的團隊非同步協作。

### 4. 響應式介面優化 (Mobile RWD Refactoring)
*   **捨棄行內樣式與 JS 監聽**：將原本寫在 `App.tsx` 內大量且吃效能的 Inline-styles 與 `window.resize` 監聽器移除。
*   **純 CSS 彈性佈局**：全面改寫 `index.css`，採用標準的 Media Query。
*   **手機版最佳化**：
    *   當螢幕小於 `980px` 時，自動消除過寬的左右留白邊界。
    *   版面由「左右並排」自動切換為符合手機滑動習慣的「上下堆疊（上方表單、下方地圖）」。
    *   動態縮小手機版字體與優化地圖高度 (`480px`)，達成近似原生 App 的操作手感。

---

## 📋 後續協作團隊待辦事項 (Action Items)

1. **資料庫建立 (Supabase)**：請後端負責人依照 `walkthrough.md` 提供的最新 SQL 語法，在 Supabase 建立 `messages` 資料表（需包含 `nickname` 與 `avatar` 欄位），以正式啟用留言板的永久儲存功能。
2. **圖資細節確認**：地籍圖層 (Land_0452) 需在較高的放大倍率 (Zoom In) 才能清楚看見線條，後續若需「帶有地號文字」的清晰圖磚，需依法向內政部國土測繪中心申請 API 權限。
3. **申請專案專用 Google Maps API 金鑰**：目前開發環境已串接 Google Maps Geocoding API 解決偏鄉門牌定位問題。請協作負責人使用專案團隊的 Google 帳號，前往 Google Cloud Console 申請正式的 API 金鑰（需綁定專案專用信用卡以啟用每月 200 美金免費額度），並將取得的字串更新至正式主機的環境變數 (`VITE_GOOGLE_MAPS_API_KEY`) 中。
